import { connect } from "react-redux";
import EditUserForm from "./user_show";

// current user info
// errors
const mSTP = (state, ownProps) => ({

});


// clear errors action
// edit user action
// delete user action
const mDTP = dispatch => ({

});

export default connect(mSTP, mDTP)(EditUserForm);